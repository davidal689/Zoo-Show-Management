<UserControl x:Class="Zoo_Show_Mnm.Views.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Zoo_Show_Mnm.Views"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800">
    <Grid Background="#F0F0F0">
        <Border Background="White" BorderBrush="DarkGray" BorderThickness="1" CornerRadius="5"
                Width="400" Padding="25" VerticalAlignment="Center" HorizontalAlignment="Center">
            <StackPanel>
                <!-- Title -->
                <TextBlock Text="AquaZoo Shows" FontSize="24" FontWeight="Bold" Foreground="Black" HorizontalAlignment="Center" Margin="0,0,0,5"/>
                <TextBlock Text="Hệ Thống Quản Lý Lịch Biểu" FontSize="12" Foreground="Gray" HorizontalAlignment="Center" Margin="0,0,0,20"/>

                <!-- Message boxes -->
                <TextBlock x:Name="TxtError" Foreground="Red" FontSize="12" FontWeight="SemiBold" TextWrapping="Wrap" HorizontalAlignment="Center" Margin="0,0,0,10" Visibility="Collapsed"/>
                <TextBlock x:Name="TxtSuccess" Foreground="Green" FontSize="12" FontWeight="SemiBold" TextWrapping="Wrap" HorizontalAlignment="Center" Margin="0,0,0,10" Visibility="Collapsed"/>

                <!-- Login panel -->
                <StackPanel x:Name="PanelLogin">
                    <Label Content="Tên đăng nhập (Username)" Foreground="Black" Margin="0,0,0,2"/>
                    <TextBox x:Name="TxtEmail" Height="28" VerticalContentAlignment="Center" Margin="0,0,0,10"/>

                    <Label Content="Mật khẩu" Foreground="Black" Margin="0,0,0,2"/>
                    <!-- Password Switchable Grid -->
                    <Grid Margin="0,0,0,5">
                        <PasswordBox x:Name="TxtPassword" Height="28" VerticalContentAlignment="Center"/>
                        <TextBox x:Name="TxtPasswordVisible" Height="28" VerticalContentAlignment="Center" Visibility="Collapsed"/>
                    </Grid>
                    
                    <CheckBox x:Name="ChkShowPassword" Content="Hiện mật khẩu" Margin="0,0,0,15" Checked="ChkShowPassword_Checked" Unchecked="ChkShowPassword_Unchecked"/>

                    <Button Content="Đăng Nhập" Click="BtnLogin_Click" Height="32" Margin="0,0,0,10" FontWeight="Bold"/>
                    <Button Content="Đăng ký tài khoản Visitor" Click="BtnToggleRegister_Click" Height="32"/>
                </StackPanel>

                <!-- Registration panel -->
                <StackPanel x:Name="PanelRegister" Visibility="Collapsed">
                    <Label Content="Họ và Tên" Foreground="Black" Margin="0,0,0,2"/>
                    <TextBox x:Name="TxtRegName" Height="28" VerticalContentAlignment="Center" Margin="0,0,0,10"/>

                    <Label Content="Tên đăng nhập mới (Username)" Foreground="Black" Margin="0,0,0,2"/>
                    <TextBox x:Name="TxtRegEmail" Height="28" VerticalContentAlignment="Center" Margin="0,0,0,10"/>

                    <Label Content="Mật khẩu" Foreground="Black" Margin="0,0,0,2"/>
                    <Grid Margin="0,0,0,5">
                        <PasswordBox x:Name="TxtRegPassword" Height="28" VerticalContentAlignment="Center"/>
                        <TextBox x:Name="TxtRegPasswordVisible" Height="28" VerticalContentAlignment="Center" Visibility="Collapsed"/>
                    </Grid>

                    <Label Content="Xác nhận mật khẩu" Foreground="Black" Margin="0,0,0,2"/>
                    <Grid Margin="0,0,0,5">
                        <PasswordBox x:Name="TxtRegConfirmPassword" Height="28" VerticalContentAlignment="Center"/>
                        <TextBox x:Name="TxtRegConfirmPasswordVisible" Height="28" VerticalContentAlignment="Center" Visibility="Collapsed"/>
                    </Grid>

                    <CheckBox x:Name="ChkRegShowPassword" Content="Hiện mật khẩu" Margin="0,0,0,15" Checked="ChkRegShowPassword_Checked" Unchecked="ChkRegShowPassword_Unchecked"/>

                    <Button Content="Đăng Ký" Click="BtnRegister_Click" Height="32" Margin="0,0,0,10" FontWeight="Bold"/>
                    <Button Content="Quay lại Đăng Nhập" Click="BtnToggleLogin_Click" Height="32"/>
                </StackPanel>

                <!-- Change Password (Temp Pwd) panel -->
                <StackPanel x:Name="PanelChangePassword" Visibility="Collapsed">
                    <TextBlock Text="Cập nhật mật khẩu tạm thời" FontSize="14" FontWeight="Bold" Foreground="Orange" Margin="0,0,0,5" HorizontalAlignment="Center"/>
                    <TextBlock Text="Tài khoản của bạn được cấp mật khẩu tạm. Hãy đổi mật khẩu để tiếp tục." FontSize="11" Foreground="Gray" Margin="0,0,0,15" TextWrapping="Wrap" HorizontalAlignment="Center"/>

                    <Label Content="Mật khẩu mới" Foreground="Black" Margin="0,0,0,2"/>
                    <Grid Margin="0,0,0,5">
                        <PasswordBox x:Name="TxtNewPassword" Height="28" VerticalContentAlignment="Center"/>
                        <TextBox x:Name="TxtNewPasswordVisible" Height="28" VerticalContentAlignment="Center" Visibility="Collapsed"/>
                    </Grid>

                    <Label Content="Xác nhận mật khẩu mới" Foreground="Black" Margin="0,0,0,2"/>
                    <Grid Margin="0,0,0,5">
                        <PasswordBox x:Name="TxtConfirmNewPassword" Height="28" VerticalContentAlignment="Center"/>
                        <TextBox x:Name="TxtConfirmNewPasswordVisible" Height="28" VerticalContentAlignment="Center" Visibility="Collapsed"/>
                    </Grid>

                    <CheckBox x:Name="ChkNewShowPassword" Content="Hiện mật khẩu" Margin="0,0,0,15" Checked="ChkNewShowPassword_Checked" Unchecked="ChkNewShowPassword_Unchecked"/>

                    <Button Content="Cập Nhật &amp; Đăng Nhập" Click="BtnChangePassword_Click" Height="32" FontWeight="Bold"/>
                </StackPanel>

                <Separator Margin="0,15,0,10"/>
                
                <!-- Reminder list -->
                <TextBlock Text="Tài khoản mặc định thử nghiệm:" FontWeight="Bold" FontSize="11" Foreground="Gray" Margin="0,0,0,4"/>
                <TextBlock Text="- Admin: admin / admin123" FontSize="10.5" Foreground="DimGray"/>
                <TextBlock Text="- Manager: manager / manager123" FontSize="10.5" Foreground="DimGray"/>
                <TextBlock Text="- Cashier: cashier / cashier123" FontSize="10.5" Foreground="DimGray"/>
                <TextBlock Text="- Visitor: visitor / visitor123" FontSize="10.5" Foreground="DimGray"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
